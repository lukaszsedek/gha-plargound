name: "01 - Building Blocks"
on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch name'
        required: true
        default: 'main'
        type: choice
        options:
        - main

jobs:
  echo-hello:
    runs-on: ubuntu-latest
    steps:
      - name: "Say hello"
        run: |
          echo "Hellow World"  

  say-goodbye:
    runs-on: ubuntu-latest
    steps:
      - name: "Failed step"
        run: |
          echo "I will fail"
          exit 1
